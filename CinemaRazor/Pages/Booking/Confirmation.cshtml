@page
@model CinemaRazor.Pages.Booking.ConfirmationModel

@{
    ViewData["Title"] = "Подтверждение бронирования";
}

<section class="page-header mb-4">
    <h1 class="page-title">Билет успешно забронирован</h1>
    <p class="page-subtitle">Ваше бронирование подтверждено и сохранено в системе</p>
</section>

<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="surface-card surface-card--loose details-container">
            <div class="details-header">
                <h2 class="details-title">Детали вашего билета</h2>
                <p class="details-subtitle">Сохраните номер билета для получения на кассе</p>
            </div>

            <div class="details-row">
                <div class="details-label">Фильм:</div>
                <div class="details-value fw-semibold">@Model.Ticket.Session.Movie.Title</div>
            </div>

            <div class="details-row">
                <div class="details-label">Жанр:</div>
                <div class="details-value">
                    <span class="badge rounded-pill text-bg-light border">@Model.Ticket.Session.Movie.Genre.Name</span>
                </div>
            </div>

            <div class="details-row">
                <div class="details-label">Дата и время:</div>
                <div class="details-value">@Model.Ticket.Session.StartTime.ToString("dd.MM.yyyy HH:mm")</div>
            </div>

            <div class="details-row">
                <div class="details-label">Место:</div>
                <div class="details-value fw-semibold">Ряд @Model.Ticket.Seat.RowNumber, Место @Model.Ticket.Seat.SeatNumber</div>
            </div>

            <div class="details-row">
                <div class="details-label">Цена:</div>
                <div class="details-value">
                    <span class="price-badge">@Model.Ticket.Price.ToString("0") BYN</span>
                </div>
            </div>

            <div class="details-row">
                <div class="details-label">Дата покупки:</div>
                <div class="details-value">@Model.Ticket.PurchaseDate.ToString("dd.MM.yyyy HH:mm")</div>
            </div>

            <div class="details-row">
                <div class="details-label">Номер билета:</div>
                <div class="details-value"><code class="fs-5">#@Model.Ticket.Id</code></div>
            </div>

            <div class="alert alert-info mt-4">
                <strong>Важная информация:</strong> Пожалуйста, сохраните номер билета. Приходите за 15 минут до начала сеанса для получения билета на кассе.
            </div>

            <div class="details-actions">
                <a asp-page="./Index" class="btn btn-primary">
                    Забронировать еще билет
                </a>
                <a asp-page="/Index" class="btn btn-outline-secondary">
                    На главную
                </a>
            </div>
        </div>
    </div>
</div>
