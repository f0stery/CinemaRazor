@page
@model CinemaRazor.Pages.Booking.ConfirmationModel

@{
    ViewData["Title"] = "Подтверждение бронирования";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-lg">
                <div class="card-header bg-success text-white text-center">
                    <h2>✅ Билет успешно забронирован!</h2>
                </div>
                <div class="card-body p-4">
                    <div class="text-center mb-4">
                        <div class="display-1">🎫</div>
                    </div>

                    <h4 class="text-center mb-4">Детали вашего билета:</h4>

                    <table class="table table-borderless">
                        <tbody>
                            <tr>
                                <td class="fw-bold text-end" style="width: 40%;">Фильм:</td>
                                <td>@Model.Ticket.Session.Movie.Title</td>
                            </tr>
                            <tr>
                                <td class="fw-bold text-end">Жанр:</td>
                                <td>@Model.Ticket.Session.Movie.Genre.Name</td>
                            </tr>
                            <tr>
                                <td class="fw-bold text-end">Дата и время:</td>
                                <td>@Model.Ticket.Session.StartTime.ToString("dd.MM.yyyy HH:mm")</td>
                            </tr>
                            <tr>
                                <td class="fw-bold text-end">Место:</td>
                                <td>Ряд @Model.Ticket.Seat.RowNumber, Место @Model.Ticket.Seat.SeatNumber</td>
                            </tr>
                            <tr>
                                <td class="fw-bold text-end">Цена:</td>
                                <td>
                                    <span class="badge bg-success fs-5">@Model.Ticket.Price.ToString("0") BYN</span>
                                </td>
                            </tr>
                            <tr>
                                <td class="fw-bold text-end">Дата покупки:</td>
                                <td>@Model.Ticket.PurchaseDate.ToString("dd.MM.yyyy HH:mm")</td>
                            </tr>
                            <tr>
                                <td class="fw-bold text-end">Номер билета:</td>
                                <td><code>#@Model.Ticket.Id</code></td>
                            </tr>
                        </tbody>
                    </table>

                    <hr class="my-4" />

                    <div class="alert alert-info">
                        <strong>📌 Важно:</strong> Пожалуйста, сохраните номер билета. Приходите за 15 минут до начала сеанса.
                    </div>

                    <div class="text-center mt-4">
                        <a asp-page="./Index" class="btn btn-primary btn-lg me-2">
                            Забронировать еще билет
                        </a>
                        <a asp-page="/Index" class="btn btn-secondary btn-lg">
                            На главную
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
