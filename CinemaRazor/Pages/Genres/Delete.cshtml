@page
@model CinemaRazor.Pages.Genres.DeleteModel

@{
    ViewData["Title"] = "Удаление жанра";
}

<section class="page-header mb-4">
    <h1 class="page-title">Удалить жанр</h1>
    <p class="page-subtitle">Вы уверены, что хотите удалить этот жанр?</p>
</section>

<div class="delete-warning mb-4">
    <h3>Внимание!</h3>
    <p>Это действие необратимо. Удаление жанра может повлиять на связанные с ним фильмы.</p>
</div>

<div class="surface-card surface-card--loose details-container">
    <div class="details-header">
        <h2 class="details-title">@Model.Genre.Name</h2>
    </div>

    <div class="details-row">
        <div class="details-label">Название:</div>
        <div class="details-value fw-semibold">@Model.Genre.Name</div>
    </div>

    <div class="details-row">
        <div class="details-label">Описание:</div>
        <div class="details-value">@(!string.IsNullOrWhiteSpace(Model.Genre.Description) ? Model.Genre.Description : "Описание отсутствует")</div>
    </div>

    <form method="post">
        <input type="hidden" asp-for="Genre.Id" />
        <div class="details-actions">
            <button type="submit" class="btn btn-danger">Удалить жанр</button>
            <a asp-page="Index" class="btn btn-outline-secondary">Отмена</a>
        </div>
    </form>
</div>
