@page
@model CinemaRazor.Pages.Sessions.IndexModel

@{
    ViewData["Title"] = "Сеансы";
}

<section class="page-header mb-4">
    <h1 class="page-title">Расписание сеансов</h1>
    <p class="page-subtitle">Планируйте показы фильмов, устанавливайте цены и контролируйте загруженность зала.</p>
</section>

<div class="d-flex justify-content-between align-items-center mb-3 flex-wrap gap-2">
    <span class="text-muted">Всего сеансов: @Model.Session.Count</span>
    <a asp-page="Create" class="btn btn-primary">Создать новый сеанс</a>
</div>

@if (Model.Session.Any())
{
    <div class="table-container">
        <div class="table-responsive">
            <table class="table align-middle">
                <thead>
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.Session[0].Movie)</th>
                        <th>Зал</th>
                        <th>@Html.DisplayNameFor(model => model.Session[0].StartTime)</th>
                        <th>@Html.DisplayNameFor(model => model.Session[0].Price)</th>
                        <th class="text-end">Действия</th>
                    </tr>
                </thead>
                <tbody>
                @foreach (var item in Model.Session)
                {
                    <tr>
                        <td class="fw-semibold">@item.Movie?.Title</td>
                        <td class="text-muted">Основной зал</td>
                        <td>@item.StartTime.ToString("dd.MM.yyyy HH:mm")</td>
                        <td>@item.Price.ToString("0") BYN</td>
                        <td class="text-end">
                            <div class="btn-group btn-group-sm" role="group">
                                <a class="btn btn-outline-secondary" asp-page="./Details" asp-route-id="@item.Id">Просмотр</a>
                                <a class="btn btn-outline-secondary" asp-page="./Edit" asp-route-id="@item.Id">Изменить</a>
                                <a class="btn btn-outline-secondary" asp-page="./Delete" asp-route-id="@item.Id">Удалить</a>
                            </div>
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    </div>
}
else
{
    <div class="support-card">
        <h2>Сеансы не найдены</h2>
        <p>Создайте первый сеанс, чтобы начать продавать билеты.</p>
        <a asp-page="Create" class="btn btn-primary">Создать сеанс</a>
    </div>
}
